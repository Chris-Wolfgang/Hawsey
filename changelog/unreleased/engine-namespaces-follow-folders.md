type: breaking

Engine types moved from the flat `Wolfgang.Hawsey.Engine` namespace into per-area namespaces (`Wolfgang.Hawsey.Engine.Cards`, `.Bidding`, `.Game`, `.Players`, `.Rules`, `.Scoring`, `.Strategy`, `.TrickPlay`); update `using` directives accordingly.
